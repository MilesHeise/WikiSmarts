<!DOCTYPE html>
<html>
  <head>
    <title>Blocipedia</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>

    <div class="container">
      <ul class="nav nav-tabs">
        <li><%= link_to "Blocipedia", welcome_index_path %></li>
        <li><%= link_to "About", welcome_about_path %></li>
      </ul>

      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <% if user_signed_in? %>
        Signed in as:
        <strong><%= current_user.email %></strong>
        |
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %>
        -
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link' %>
        -
        <%= render partial: 'oh no where should this go?', locals: { user: current_user } %>
      <% else %>
        <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link' %>
        -
        <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link' %>
      <% end %>

      <%= yield %>
    </div>

  </body>
</html>

<!-- content for oh no where should this go?

<% if user.role == :premium %>
  <%= link_to [user], class: 'btn btn-danger', method: :set_standard_role do %>
    Downgrade Membership?
    ## need to write a warning for this as well in next checkpoint that says
    what will happen if they downgrade ##
  <% end %>
  <% else %>
    <%= link_to new_charge_path, class: 'btn btn-primary' do %>
      Upgrade to Premium?
  <% end %>
<% end %> -->
